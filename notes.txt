-------
В либе Parchment есть бага в функции query, ее надо манкипатчить в уже сбилженном dist/quill.js
// else if (query instanceof HTMLElement) {
// Doesn't work in chrome, so we duck type
else {
-------